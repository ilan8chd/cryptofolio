{
  "name": "server",
  "type": "module",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "node --env-file='.env.development.local' --watch src/index.js",
    "lint": "eslint src",
    "start": "node --env-file-if-exists='.env' src/index.js"
  },
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "fastify": "^5.6.1",
    "mongoose": "^9.2.4"
  },
  "devDependencies": {
    "pino-pretty": "^13.1.3"
  }
}
